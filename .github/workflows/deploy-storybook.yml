name: Deploy Storybook to GitHub Pages

on:
  push:
    branches:
      - release # 每次 push 到 release 时触发部署

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install

      - name: Build Storybook
        run: pnpm build-storybook
        working-directory: apps/docs

      - name: Add .nojekyll to bypass Jekyll on GitHub Pages
        run: touch storybook-static/.nojekyll # 添加 （跳过）.nojekyll 文件 GitHub Pages 默认会用 Jekyll 去构建你的仓库，但你的仓库不是 Jekyll 项目（它是前端组件库 Monorepo）。
        working-directory: apps/docs

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: apps/docs/storybook-static